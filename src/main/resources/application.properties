# 1. Only the Dev Profile
spring.profiles.active=dev

# 2. Global Settings
spring.application.name=saas
spring.threads.virtual.enabled=true

# 3. Common JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true